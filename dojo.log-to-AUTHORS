#!/usr/bin/env python
# Usage: dojo.log-to-AUTHORS <dojo.log >AUTHORS
# This script helps generating a list of participants of a dojo session based on
# the log file generated by DojoTimer.

import sys


authors = set()

for line in sys.stdin:
    try:
        authors.update(map(str.strip, line.split(' *** ')[1].split(' and ', 2)))
    except:
        raise

print('\n'.join(sorted(authors)))
